# shared/hooks

Generic, domain-free React hooks used by more than one feature (media queries, focus
trapping, click-outside, debounced values).

Anything that knows about auth, rooms, games, chat or friends is feature work and
belongs in that feature's `hooks/` folder instead.
