/**
 * Lobby screen shell.
 *
 * Entry points only: current user, create room, join room by code, profile, friends.
 * There is no room browser — Join Room is code entry, by contract.
 */
export function LobbyPage() {
  return (
    <section>
      <h1>Lobby</h1>
      {/* TODO(lobby): current user, create-room entry, join-by-code entry, friends. */}
    </section>
  );
}
