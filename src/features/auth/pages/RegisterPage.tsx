/** Registration screen shell. Register requires email, username and password. */
export function RegisterPage() {
  return (
    <section>
      <h1>Create account</h1>
      {/* TODO(auth): registration form, INLINE_VALIDATION errors, redirect to the Lobby. */}
    </section>
  );
}
