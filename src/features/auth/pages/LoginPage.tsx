/** Login screen shell. Cookie-only auth: never store a token in the browser. */
export function LoginPage() {
  return (
    <section>
      <h1>Log in</h1>
      {/* TODO(auth): login form, INLINE_VALIDATION errors, redirect to the Lobby. */}
    </section>
  );
}
