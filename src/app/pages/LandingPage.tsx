/** Landing screen shell. Entry point into Login/Register. */
export function LandingPage() {
  return (
    <section>
      
      {/* TODO(design): landing content and the Login/Register entry points. */}
    </section>
  );
}
