info:
  name: "game_action_logged"
  type: websocket
  seq: 10
  tags:
    - event
    - observation
websocket:
  url: "{{wsUrl}}/ws?session_id={{session_id}}"
  message:
    - title: "game_action_logged — observation example"
      selected: false
      message:
        type: text
        data: "<observe the game_action_logged server frame; no outgoing message>"
settings:
  timeout: 30000
  keepAliveInterval: 0
docs: |-
  # ⚡ game_action_logged

  ## Purpose

  Passive observer for the server event `game_action_logged`. This is an action-flow event associated with a recorded game action.

  ## URL and authentication

  `{{wsUrl}}/ws?session_id={{session_id}}`

  The session is supplied in the `session_id` query parameter. Open `connect` first; this request does not create a second connection workflow.

  ## Message behavior

  No outgoing application message is sent. Watch Bruno's message history for the `game_action_logged` frame after the related room or game action.

  ## Payload boundary

  The event name is documented, but no payload fields are added here unless confirmed by the backend contract. Do not treat a guessed payload as an API example.

  ## Validation

  Compare the captured event name and ordering with the action or connection-state change that triggered it. Record unexpected frames for backend investigation.
