info:
  name: "reveal_card"
  type: websocket
  seq: 4
  tags:
    - action
    - manual
websocket:
  url: "{{wsUrl}}/ws?session_id={{session_id}}"
  message:
    - title: "reveal_card — payload schema to be confirmed"
      selected: false
      message:
        type: text
        data: "<replace with the contract-confirmed reveal_card data payload before enabling>"
docs: |-
  # `reveal_card`

  Client event name: `reveal_card`.

  The backend determines the result; clients must never send `card_type`, `correct`, or another predicted result. The field-level payload schema was not provided, so this message is disabled.

