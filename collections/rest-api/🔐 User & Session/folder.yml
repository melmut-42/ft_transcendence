info:
  name: 🔐 User & Session
  type: folder

docs:
  content: |-
    # 👤 User & Session

    This folder contains the temporary identity bootstrap used by the rest of the workspace. 👤 It creates or enters a username-based session so later room requests can be authenticated without introducing passwords, JWTs, or unsupported account-management behavior.

    ## 🧭 When to use these requests

    - Run `Enter user` first to create the primary temporary user.
    - Use the prepared `guest_session_id` environment variable when a room workflow needs another participant.
    - Inspect the native `400 INVALID_REQUEST` example to verify validation behavior.
    - Inspect the native `409 USERNAME_TAKEN` example to verify the exact conflict contract.

    ## 🔗 Runtime chaining

    Successful responses populate session and user identifiers in Bruno runtime variables. Those values are consumed by the Rooms folder and are not written to the committed environment file.

    > 🔐 **Security**
    >
    > Session IDs are temporary secrets. Keep them in Bruno's local environment storage and leave committed secret variables blank.

    ## 🧪 Testing focus

    Verify the `201`, `400 INVALID_REQUEST`, and `409 USERNAME_TAKEN` examples. The native examples are safe to inspect without sending a request.
  type: text/markdown
