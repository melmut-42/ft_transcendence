info:
  name: 04 - Error & State Examples
  type: folder
  seq: 4
docs: |-
  # Error and state examples

  Cross-endpoint REST taxonomy and canonical room lifecycle index. Endpoint files remain authoritative for applicability; this folder prevents unrelated errors from being attached to every request.

  ## Error envelope

  ```json
  {"error":{"code":"ROOM_NOT_FOUND","message":"Room 9999 was not found.","details":{"room_id":9999}}}
  ```

  | HTTP | Code | Concrete message example | Applicable operations |
  | ---: | --- | --- | --- |
  | `422` | `INVALID_USERNAME` | `Username must match ^[A-Za-z0-9_]{3,20}$.` | register |
  | `409` | `USERNAME_TAKEN` | `Username 'player_one' is already registered.` | register |
  | `401` | `UNAUTHORIZED` | `A valid active session is required.` | every protected endpoint |
  | `404` | `USER_NOT_FOUND` | `User 9999 was not found.` | public profile |
  | `404` | `ROOM_NOT_FOUND` | `Room 9999 was not found.` | join, get, leave |
  | `409` | `ROOM_NOT_JOINABLE` | `Room 1001 is not joinable while status is COUNTDOWN.` | join |
  | `409` | `ALREADY_IN_ROOM` | `User 46 already belongs to active room 1002.` | create, join |
  | `403` | `NOT_ROOM_MEMBER` | `User 47 is not a member of room 1001.` | get, leave |
  | `409` | `INVALID_ROOM_STATE` | `Room 1001 cannot be left while status is IN_GAME.` | leave |
  | `400` or `422` | `VALIDATION_ERROR` | `user_id must be a positive integer.` | malformed JSON or field/path validation |
  | `429` | `RATE_LIMITED` | `Too many registration attempts; retry after 60 seconds.` | throttled operation |

  Malformed JSON uses `400 VALIDATION_ERROR`; syntactically valid values that fail field constraints use `422 VALIDATION_ERROR`, except the more specific `INVALID_USERNAME`. Every error keeps state unchanged.

  ## State example index

  `Get room snapshot` contains new, four-player unconfigured, incomplete-role, valid-role/unready, full `IN_GAME` Operative, missing-room, and forbidden-access examples. WebSocket scenario docs contain all-ready, countdown, cancellation, Spymaster `IN_GAME`, every reveal branch, and finished state.
