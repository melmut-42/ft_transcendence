info:
  name: "Join finished room"
  type: http
  seq: 14
  tags:
    - manual
    - negative
http:
  method: POST
  url: "{{baseUrl}}/rooms/join"
  headers:
    - name: X-Session-Id
      value: "{{outsider_session_id}}"
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "room_id": {{prepared_finished_room_id}}
      }
runtime:
  scripts:
    - type: tests
      code: |-
        test("rejects joining a finished room", function() {
          expect(res.status).to.equal(409);
          expect(res.body.error.code).to.equal("ROOM_NOT_JOINABLE");
        });
examples:
  - name: "409 ROOM_NOT_JOINABLE"
    response:
      status: 409
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": {
              "code": "ROOM_NOT_JOINABLE"
            }
          }
docs: |-
  # Finished room

  **Manual scenario.** Set `prepared_finished_room_id` to a valid `FINISHED` room. Expected response: `409 ROOM_NOT_JOINABLE`.
